
import 'package:flutter/material.dart';

class ScanPanel extends StatelessWidget{
  const ScanPanel({super.key});
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("camera"));
  }
}